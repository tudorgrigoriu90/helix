{
  "name": "@helix/content",
  "version": "0.0.0",
  "private": true,
  "description": "Game content as JSON: mutations, enemies, items, floors, lace lines, organism names",
  "type": "module",
  "scripts": {
    "validate": "echo 'run content validation from the repo root: pnpm validate (gate lives in @helix/game — it needs the loaders)'"
  }
}
